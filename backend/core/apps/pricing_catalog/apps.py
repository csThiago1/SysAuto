from django.apps import AppConfig


class PricingCatalogConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.pricing_catalog"
    verbose_name = "Catálogo de Precificação"
